language: node_js
node_js:
 - "node"
cache:
  directories:
    - "node_modules"
jobs:
  include:
    - stage: lint
      script: npm run lint
    - stage: test
      script: npm test
stages:
  - lint
  - test
after_success: "npm run coveralls"
