language: node_js
node_js:
 - "node"
install:
 - npm ci
cache:
  directories:
    - "node_modules"
jobs:
  include:
    - install
    - stage: lint
      script: npm run lint
    - stage: test
      script: npm test
stages:
  - lint
  - test
after_success: "npm run coveralls"
