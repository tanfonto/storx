{
  "name": "storx",
  "version": "0.0.1",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "test:dev": "tsc && nyc ava --tap | tap-diff",
    "test": "tsc && nyc ava && nyc report --reporter=text-lcov | coveralls",
    "watch": "ava --verbose --watch"
  },
  "author": "Whapky",
  "license": "ISC",
  "dependencies": {
    "@types/ramda": "^0.25.38",
    "ava": "^0.25.0",
    "ava-ts": "^0.25.2",
    "coveralls": "^3.0.2",
    "nyc": "^13.1.0",
    "ramda": "^0.25.0",
    "ramda-adjunct": "^2.10.0",
    "rxjs": "^6.3.3",
    "tap-diff": "^0.1.1",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.1"
  }
}
